// 语义检查
// 上下文语义分析
#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include "ring.h"

// 语义检查
// 上下文语义分析
int semantic_check(Ring_Interpreter *ring_interpreter) {
    assert(ring_interpreter != NULL);

    // exit(1);
    return 0;
}
