// 语义检查
// 上下文语义分析
#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include "ring.h"

// 语义检查
// 上下文语义分析
int ring_semantic_check(Ring_Compiler *ring_compiler) {
    assert(ring_compiler != NULL);

    // exit(1);
    return 0;
}
